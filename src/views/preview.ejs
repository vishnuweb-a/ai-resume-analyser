<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/resume.css">
</head>
<body>

<div class="resume">

  <h1><%= resume.name %></h1>

  <% if (resume.summary) { %>
    <div class="section">
      <h2>Professional Summary</h2>
      <p><%= resume.summary %></p>
    </div>
  <% } %>

  <% if (resume.skills) { %>
    <div class="section">
      <h2>Skills</h2>
      <pre><%= resume.skills %></pre>
    </div>
  <% } %>

  <% if (resume.experience) { %>
    <div class="section">
      <h2>Experience</h2>
      <p><%= resume.experience %></p>
    </div>
  <% } %>

  <% if (resume.education) { %>
    <div class="section">
      <h2>Education</h2>
      <p><%= resume.education %></p>
    </div>
  <% } %>

  <a class="edit-link" href="/resume/ai">← Back</a>

<br><br>
<a href="/resume/download">⬇ Download as PDF</a>

</div>

</body>
</html>
